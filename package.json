{
  "name": "nanostores-firebase-starter",
  "packageManager": "yarn@4.0.1",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "start-firebase": "npx firebase emulators:start --only firestore,auth,functions,storage --project demo-nanostores"
  }
}
